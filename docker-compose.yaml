services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: brewMe
      POSTGRES_USER: brewme
      POSTGRES_PASSWORD: brewme
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
